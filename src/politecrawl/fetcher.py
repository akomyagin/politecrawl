"""HTTP-загрузка страниц и извлечение ссылок.

Этап 1: базовый asyncio-fetch через httpx.AsyncClient + парсинг ссылок.
"""

from __future__ import annotations

# TODO(Этап 1): реализовать async fetch(url) -> тело/статус через httpx.AsyncClient
# TODO(Этап 1): реализовать extract_links(base_url, html) -> list[str] (абсолютные URL)
