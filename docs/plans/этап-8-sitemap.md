# Этап 8 — Sitemap: из robots.txt: план (черновик / handoff)

**Статус:** планировщик (Opus-агент) был прерван по лимиту контекста сессии
до того, как успел написать этот файл в стиле `docs/plans/этап-7-export.md`
(конкретные сигнатуры, куски кода, порядок объявлений, тест-кейсы). НЕ
считать это финальным планом — это заметка для продолжения в следующей
сессии.

## Что уже есть

Полная спецификация с сигнатурами кода, дизайн-решениями и перечнем
тест-кейсов по модулям уже записана в
**`docs/TECHNICAL_PLAN.md` §«Этап 8 — Sitemap: из robots.txt (POST_MVP)»**
(коммит `80bc68b`). Она включает:

- контракт `RobotsCache.sitemaps()` (готовый сниппет кода);
- контракт `extract_sitemap_urls()` в `fetcher.py` (сигнатура + докстринг,
  возвращает `(page_urls, nested_sitemap_urls)`);
- интеграцию в `Crawler._process`/`_discover_sitemaps` (готовый сниппет);
- решение по глубине sitemap-URL (`depth=0`) с обоснованием;
- решение по `<sitemapindex>` (один уровень, без рекурсии — POST_MVP);
- решение по gzip-sitemap (не поддерживается — POST_MVP);
- новый счётчик `CrawlStats.sitemap_urls`;
- список имён тест-кейсов для `tests/test_robots.py`, `tests/test_fetcher.py`,
  `tests/test_cli.py` (без кода тестов, только что каждый должен проверять);
- границы («что НЕ входит») и критерий «готово когда».

## Что нужно сделать при продолжении

1. Перечитать `docs/TECHNICAL_PLAN.md` §«Этап 8» целиком.
2. **Либо** развернуть его в подробный план `docs/plans/этап-8-sitemap.md`
   (перезаписав этот черновик) — по глубине/стилю как `docs/plans/этап-7-export.md`
   (готовые куски кода для каждого файла, точный порядок объявлений, тест-кейсы
   с телами, а не только именами) — отдельным Agent-вызовом `model: "opus"|"fable"`;
   **либо**, если экономим цикл, передать секцию TECHNICAL_PLAN.md напрямую
   кодеру (`model: "fable"`) как достаточно детальную спецификацию — она уже
   содержит рабочие сниппеты для `robots.py`/`fetcher.py`/`cli.py`.
3. Дальше — по пайплайну проекта (см. `CLAUDE.md`): кодинг → тесты/линт/типы
   (основная сессия) → независимое Opus-ревью → цикл исправлений → commit/push/PR.

## Технический контекст (на случай, если ветка/файлы не сохранились)

- Ветка: `этап-8-robots-sitemap`, создана от актуального `master` (после мержа
  PR #8, Этап 7 — экспорт результатов).
- Ничего в `src/`/`tests/` ещё не тронуто — весь Этап 8 ещё предстоит написать.
- Задачи (TaskList) этой сессии: #7 (план, не завершена) … #12 (PR) заведены,
  но не выполнены дальше планирования.
