# Handoff — 2026-08-22: Этапы 7 и 8 завершены

## Состояние

- `master` актуален, рабочее дерево чистое, ничего не в работе.
- Этап 7 (экспорт результатов: рёбра/sitemap/метаданные) — [PR #8](https://github.com/akomyagin/politecrawl/pull/8), смёржен.
- Этап 8 (Sitemap: из robots.txt) — [PR #9](https://github.com/akomyagin/politecrawl/pull/9), смёржен.
- Локальные и удалённые ветки этапов 3-8 удалены (все смёржены, `git branch -a`
  сейчас показывает только `master`).
- Пайплайн (Opus-план → Fable-код → Sonnet-проверка тестов/линта/типов →
  независимое Opus-ревью → разбор находок → commit/push/PR) отработал дважды
  подряд без сбоев — детали см. `[[feedback_stage_cadence]]` в памяти.

## Что дальше

Следующий пункт **не выбран**. Кандидаты — `docs/POST_MVP_PLAN.md`:
- персистентность фронтира (возобновление обхода с чекпоинта);
- JS-рендеринг через headless-браузер (`--render`, Playwright/pyppeteer);
- распределённый краулинг (Redis-очередь + `SET`/bloom, несколько машин);
- метрики Prometheus / структурные логи.

Одна заметка: `docs/plans/этап-8-sitemap.md` изначально писался как
handoff-заметка о прерванном планировщике (контекст сессии кончился
посреди планирования) — теперь помечена как «ЗАВЕРШЕНО» и оставлена как
историческая запись о том, что реализация обошлась без развёрнутого плана
(кодер работал прямо по спеке в `TECHNICAL_PLAN.md` §«Этап 8»). Это
рабочий прецедент на случай похожего обрыва в будущем.

## Как продолжить

1. Спросить пользователя, какой пункт POST_MVP брать в работу (или
   дождаться явного указания).
2. Завести ветку `этап-9-<тема>` от `master`.
3. Дальше — по пайплайну из `CLAUDE.md` (проектный + `~/.claude/CLAUDE.md`).
